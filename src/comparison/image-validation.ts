import { MAX_IMAGE_BYTES, detectSupportedImage } from "../engine/image-validation.js";

/** Compatibility names retained for the completed comparison experiment. */
export const MAX_COMPARISON_IMAGE_BYTES = MAX_IMAGE_BYTES;
export { detectSupportedImage };
